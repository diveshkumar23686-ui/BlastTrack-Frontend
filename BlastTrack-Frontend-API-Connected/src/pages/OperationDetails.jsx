import { useEffect, useState } from "react";
import { useNavigate,useParams } from "react-router-dom";
import PageHeader from "../components/PageHeader";
import StatusBadge from "../components/StatusBadge";
import Icon from "../components/Icon";
import { operationsApi, documentsApi } from "../services/api";
export default function OperationDetails(){const {id}=useParams(),navigate=useNavigate();const [op,setOp]=useState(null),[docs,setDocs]=useState([]),[error,setError]=useState("");useEffect(()=>{Promise.all([operationsApi.get(id),documentsApi.list()]).then(([o,d])=>{setOp(o.data);setDocs(d.data.filter(x=>x.operation===id))}).catch(e=>setError(e?.response?.data?.message||"Could not load operation."))},[id]);if(error)return <div className="notice"><Icon name="shield"/><span>{error}</span></div>;if(!op)return <div className="panel">Loading...</div>;return <div><button className="back-btn" onClick={()=>navigate("/operations")}><Icon name="arrow" size={16}/> Back to Operations</button><PageHeader eyebrow="OPERATION RECORD" title={op.id} description="Completed operation record and associated administrative information." action={<StatusBadge>{op.status}</StatusBadge>}/><div className="details-grid"><section className="panel"><div className="panel-head"><div><h2>Operation information</h2><p>Record details</p></div></div><div className="detail-list">{[["Operation ID",op.id],["Date",op.date],["Location",op.location],["Responsible Officer",op.officer],["Workers",`${op.workers} recorded`],["Equipment",op.equipment||"Not specified"],["Safety Check",op.safety],["Remarks",op.remarks||"—"]].map(([k,v])=><div className="detail-row" key={k}><span>{k}</span><strong>{v}</strong></div>)}</div></section><section className="panel"><div className="panel-head"><div><h2>Record timeline</h2><p>Administrative record history</p></div></div><div className="timeline">{["Operation Record Added","Workers Recorded","Equipment Recorded","Safety Check Recorded","Operation Completed"].map((x,i)=><div className="timeline-item" key={x}><div className="timeline-dot"><Icon name={i<4?"check":"layers"} size={13}/></div><div><strong>{x}</strong><span>{i<4?"Record available":op.date}</span></div></div>)}</div></section></div><div className="details-grid lower"><section className="panel"><div className="panel-head"><div><h2>Documents</h2><p>Files linked to this record</p></div></div><div className="mini-list">{docs.length?docs.map(d=><div className="list-row" key={d.name}><div className="list-main"><div className="list-icon purple"><Icon name="file"/></div><div><strong>{d.name}</strong><span>{d.type} • {d.date}</span></div></div>{d.url?<a className="text-btn" href={d.url} target="_blank" rel="noreferrer">View</a>:null}</div>):<div className="table-empty">No documents linked.</div>}</div></section><section className="panel"><div className="panel-head"><div><h2>Record summary</h2><p>Administrative status</p></div></div><div className="summary-box"><div><span>Safety</span><StatusBadge>{op.safety}</StatusBadge></div><div><span>Completion</span><StatusBadge>{op.status}</StatusBadge></div><div><span>Workers recorded</span><strong>{op.workers}</strong></div></div></section></div></div>}
