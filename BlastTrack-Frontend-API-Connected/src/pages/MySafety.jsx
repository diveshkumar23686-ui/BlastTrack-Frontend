import { useEffect,useState } from "react";
import PageHeader from "../components/PageHeader";
import StatusBadge from "../components/StatusBadge";
import { safetyApi } from "../services/api";
export default function MySafety(){const [rows,setRows]=useState([]);useEffect(()=>{safetyApi.list().then(r=>setRows(r.data)).catch(()=>{})},[]);const passed=rows.filter(x=>x.status==="Passed").length,pending=rows.filter(x=>x.status==="Pending").length;return <div><PageHeader eyebrow="WORKER VIEW" title="Safety Records" description="View relevant safety-check records. Official records cannot be edited from this account."/><div className="stats-grid three"><div className="stat-card"><div className="stat-icon green"><span>✓</span></div><div className="stat-content"><div className="stat-title">Passed</div><div className="stat-value">{passed}</div><div className="stat-note">Live database</div></div></div><div className="stat-card"><div className="stat-icon orange"><span>!</span></div><div className="stat-content"><div className="stat-title">Pending</div><div className="stat-value">{pending}</div><div className="stat-note">Live database</div></div></div><div className="stat-card"><div className="stat-icon purple"><span>↗</span></div><div className="stat-content"><div className="stat-title">Total Records</div><div className="stat-value">{rows.length}</div><div className="stat-note">Live database</div></div></div></div><section className="panel table-panel"><div className="table-wrap"><table><thead><tr><th>Check ID</th><th>Operation</th><th>Date</th><th>Inspector</th><th>Status</th><th>Remarks</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><strong className="primary-text">{x.id}</strong></td><td>{x.operation}</td><td>{x.date}</td><td>{x.inspector}</td><td><StatusBadge>{x.status}</StatusBadge></td><td>{x.remarks}</td></tr>)}</tbody></table></div></section></div>}
